name: "WOrkflow2"

on: 
  push:
    branches:
      - test

jobs:
  WOrkflow2: # name of the job
    runs-on: ubuntu-latest #runner machine
    steps:
      - name: "Print My Name"
        run: echo "Hello Amit ARmaan"